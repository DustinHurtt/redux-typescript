const RepositoriesList: React.FC = () => {
    return <div>
        <form>
            <input aria-label="repositories search"/>
            <button>Search</button>
        </form>
    </div>
};

export default RepositoriesList